apiVersion: v1
kind: Service

metadata:
  name: login-service

spec:

  selector:
    app: login-app

  ports:
  - protocol: TCP

    port: 3000

    targetPort: 3000

    nodePort: 30080

  type: NodePort